<h2><%= link_to @post.title, post_path(@post) %> </h2>
<h4><%= @post.created_at.strftime("%b %e at %l:%M%p") %> </h4>
<%= @post.content %>

<h4>
  <%= link_to "Edit", edit_post_path(@post) %>
  <%= link_to 'Delete', @post, method: :delete, data: { confirm: 'Are you sure?' }%>
</h4>
