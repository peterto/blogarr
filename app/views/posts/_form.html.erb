<%= form_for(@post) do |f| %>

  <h1><%= f.label :title %></h1>
  <%= f.text_field :title %>
  
  <h1><%= f.label :content %></h1>
  <%= f.text_area :content %>
  
  <%= hidden_field_tag 'user_id', current_user.id %>
  <%= f.submit "Create Post" %>
<% end %>